{
    "require-dev": {
        "vimeo/psalm": "^5.15",
        "ging-dev/psalm-plugin-wordpress": "5.x-dev",
        "wp-coding-standards/wpcs": "^3.0",
        "php-stubs/woocommerce-stubs": "^8.2"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "scripts": {
        "cs": "phpcs",
        "cs-fix": "phpcbf",
        "psalm": "psalm --no-cache"
    },
    "require": {
        "bentools/webpack-encore-resolver": "^0.1.0"
    }
}
